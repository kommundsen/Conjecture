{
  "skill_name": "implement",
  "evals": [
    {
      "id": 1,
      "prompt": "I've added CharStrategyTests.cs to src/Conjecture.Tests/Strategies/ with tests for a new Gen.Chars() method. Make the failing tests pass.",
      "expected_output": "Gen.Chars() and Gen.Chars(char[] alphabet) factory methods added to Gen.cs; a minimal internal strategy class created in Conjecture.Core.Generation; all 3 CharStrategyTests pass; full dotnet test src/ shows no regressions.",
      "files": ["evals/files/CharStrategyTests.cs"],
      "expectations": [
        "All 3 tests in CharStrategyTests pass (dotnet test output shows 0 failures for CharStrategyTests)",
        "No previously passing tests are broken (full dotnet test src/ exits 0)",
        "Gen.Chars() returns only characters from the specified alphabet (verified by test Chars_CustomAlphabet_ReturnsOnlyFromAlphabet passing)",
        "Gen.cs is updated with at least one new Chars() factory method",
        "New source files (if any) use file-scoped namespace declarations (not block-scoped)"
      ]
    },
    {
      "id": 2,
      "prompt": "The tests in StringStrategyTests.cs are failing because Gen.Strings() doesn't exist yet — I've added the file to src/Conjecture.Tests/Strategies/. Implement it.",
      "expected_output": "Gen.Strings() and Gen.Strings(minLength, maxLength) and Gen.Strings(alphabet, minLength, maxLength) factory methods added; minimal string strategy class created; all 4 StringStrategyTests pass; no regressions.",
      "files": ["evals/files/StringStrategyTests.cs"],
      "expectations": [
        "All 4 tests in StringStrategyTests pass (dotnet test output shows 0 failures for StringStrategyTests)",
        "No previously passing tests are broken (full dotnet test src/ exits 0)",
        "Gen.cs is updated with at least one new Strings() factory method",
        "New source files use file-scoped namespace declarations (not block-scoped)",
        "Implementation does not add extra public API beyond what the tests reference"
      ]
    },
    {
      "id": 3,
      "prompt": "Can you make the failing tests in ListStrategyTests pass? I've added them to src/Conjecture.Tests/Strategies/ListStrategyTests.cs — Gen.ListOf() doesn't exist yet.",
      "expected_output": "Gen.ListOf<T>(Strategy<T> elementStrategy, int minLength, int maxLength) factory method added; a minimal list strategy class wrapping the element strategy created; all 4 ListStrategyTests pass; no regressions.",
      "files": ["evals/files/ListStrategyTests.cs"],
      "expectations": [
        "All 4 tests in ListStrategyTests pass (dotnet test output shows 0 failures for ListStrategyTests)",
        "No previously passing tests are broken (full dotnet test src/ exits 0)",
        "A new strategy class for list generation exists in src/Conjecture.Core/Generation/",
        "Gen.cs is updated with a new ListOf() factory method",
        "New source files use file-scoped namespace declarations (not block-scoped)"
      ]
    }
  ]
}
